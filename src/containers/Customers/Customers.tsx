import styles from "./Customers.module.css";

const Customers = () => {
  return (
    <section id="clientes" className={styles.consultancy}>
      <div className="container"></div>
    </section>
  );
};

export default Customers;
